----
import { HiArrowRight } from "react-icons/hi";

export interface Props {
  titulo: string;
  descripcion: string;
  imagen: string;
  tecnologias: string[];
  codigo: string;
  categoria?: string;
  index: number; 
}

const { titulo, descripcion, imagen, tecnologias, codigo, categoria, index } =
  Astro.props;
---

<div
  data-index={index}
  data-categoria={categoria ?? ""}
  class="project-card z-10 flex w-full max-w-sm flex-col overflow-hidden rounded-lg bg-gray-900 shadow-lg transition-all hover:scale-[1.01] drop-shadow-[4px_4px_0_#B900BC]"
>
  <img
    src={imagen}
    alt={`Imagen del proyecto ${titulo}`}
    class="h-52 w-full rounded-2xl object-cover p-3"
  />

  <div class="flex grow flex-col px-4 pb-4">
    <h3 class="text-xl font-bold text-white drop-shadow-[1px_1px_0_#B900BC]">
      {titulo}
    </h3>

    <div class="my-2 flex flex-wrap gap-2">
      {
        tecnologias.map((tech) => (
          <span class="rounded-md bg-[#00FFAB]/10 text-[#00FFAB] px-2 py-0.5 text-[10px] uppercase tracking-wider font-bold border border-[#00FFAB]/20">
            {tech}
          </span>
        ))
      }
    </div>

    <p class="mb-4 text-sm leading-relaxed text-gray-400">
      {descripcion}
    </p>

    <div class="mt-auto">
      <a
        class="group flex items-center justify-center gap-2 rounded-md border border-[#B900BC] py-2 text-sm font-bold text-[#B900BC] transition-all hover:bg-[#B900BC] hover:text-white"
        href={codigo}
        target="_blank"
        rel="noopener noreferrer"
      >
        Analizar Código
        <HiArrowRight
          className="transition-transform duration-200 group-hover:translate-x-1"
          size={16}
        />
      </a>
    </div>
  </div>
</div>